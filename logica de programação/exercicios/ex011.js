let soma = retorna(1,2);
console.log(soma)

//quando colocamos mais valores dentro de um [] ele vira um objeto isso é util portque a função so pode retorna um valor ou objeto

function retorna(num1,num2){
    let soma = [1,2,3,'1'];
    return soma
}